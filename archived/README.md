# archived/

Out-of-scope material moved here on **2026-06-23** when the project re-grounded to the
**Whiteroom** (see `docs/015-re-requirements.md`). Nothing is deleted — everything here is
recoverable (`git mv` history is intact). Pull anything back if it becomes relevant again.

**Why archived:** the project drifted into Instagram content, social video generation, the
"Monet's Garden" game concept, marketing, and a local-homelab video-gen pipeline. `docs/015`
re-scoped to *only* the whiteroom (Monet as a living being at monet.sprited.ai). These are the
artifacts of the abandoned directions.

Contents:
- `scripts/` — IG/video-gen tooling (`seedance.py`, `ig_text.py`, `ig-videos.sh`, `ui2api.py`).
- `workflows/` — local ComfyUI LTX/Wan i2v video-gen workflows.
- `docs/` — historical / IG / garden docs (`001–006`, `009`, `010`, `011-story-bites`, `013-ig-videos`).
- `experiments/` — `comfy-mcp` (ComfyUI image/video gen), `v0-template` (abandoned).
